using UnityEngine;

public class CargoFuelTank : CargoBase
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        health = 100f;
        Init();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
